import Index from "../pages/Notifications/Index";

const routes = [
    {
        path: "/notifications",
        name: "notifications",
        component: Index,
        props: true
    }
];

export default routes;
